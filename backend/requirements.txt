# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9

# ── LangChain ─────────────────────────────────────────────────────────────────
langchain==0.2.5
langchain-community==0.2.5
langchain-core==0.2.9

# ── Vector store ──────────────────────────────────────────────────────────────
faiss-cpu==1.8.0

# ── Embeddings: Better model for legal text ───────────────────────────────────
sentence-transformers==3.0.1
torch==2.3.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Reranking (cross-encoder for precision) ───────────────────────────────────
# Uses sentence-transformers cross-encoder — no extra install needed

# ── PDF parsing ───────────────────────────────────────────────────────────────
pymupdf==1.24.5

# ── Groq (free LLM + Whisper) ────────────────────────────────────────────────
openai==1.35.0
groq==0.9.0

# ── BM25 hybrid retrieval ─────────────────────────────────────────────────────
rank-bm25==0.2.2

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
